// lib/auth/auth.ts

/**
 * NextAuth has been removed.
 *
 * The admin panel now uses the custom JWT authentication system
 * based on the `rootym_admin_token` cookie.
 *
 * This file is intentionally left as a placeholder until all
 * remaining NextAuth imports have been removed.
 */

export {};